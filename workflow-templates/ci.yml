name: ci

# Caller workflow. Copy this file into <repo>/.github/workflows/ci.yml.
# Keep the job name `ci` so the org ruleset's required check "ci / ci" matches.

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  ci:
    uses: BLB3DPrinting/.github/.github/workflows/ci.yml@main
    with:
      python-version: "3.12"
      # working-directory: "."
      # run-tests: true
      # run-lint: true
